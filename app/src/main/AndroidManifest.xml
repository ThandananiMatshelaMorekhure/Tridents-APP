<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TridentSmartSolutions"
        tools:targetApi="31">
        <activity
            android:name=".ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".EditProfileActivity"
            android:exported="false" /> <!-- 🟩 LOGIN PAGE (Launcher Activity) -->
        <activity
            android:name=".LoginPage"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 🟦 SIGN-UP PAGE -->
        <activity
            android:name=".SignupPage"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- 🟨 MAIN ACTIVITY (Home after login/signup) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" /> <!-- 🟧 OTHER APP PAGES -->
        <activity
            android:name=".RequestPage"
            android:exported="false" />
        <activity
            android:name=".RequestFormActivity"
            android:exported="false" />
        <activity
            android:name=".RequestHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".ProfilePage"
            android:exported="false" />
    </application>

</manifest>